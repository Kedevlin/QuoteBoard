<section class="new_person">
  <%= form_for @person do |f| %>
    <fieldset>
      <legend>New Person</legend>
      <div>
        <%= f.label :name, "name"%>
        <%= f.text_field :name %><br />
      </div>

      <div>
        <%= f.label :bio, "bio" %>
        <%= f.text_field :bio %><br />
      </div>

      <div>
        <%= f.submit %>
      </div>
    </fieldset>
  <% end %>

</section>
